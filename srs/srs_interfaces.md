# 5. External Interface Requirements

- **REST API** (JSON over HTTP)
- **React Front-End** (Axios calls to /api/**)

# 6. Constraints

- Must use: **Spring Boot, Spring Data, MySQL, ReactJS**
- DB design must support relational integrity with foreign keys